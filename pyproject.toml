[project]
name = "flask-notepad"
version = "0.1.0"
description = "A note-taking Flask app with nested, filesystem-like folders"
requires-python = "<=3.11,>=3.10"
dependencies = [
    "flask>=3.0,<4.0",
    "flask-sqlalchemy>=3.1,<4.0",
    "flask-migrate>=4.0,<5.0",
    "python-dotenv>=1.0,<2.0",
]

[tool.uv]
package = false
